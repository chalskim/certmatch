{
  "name": "certmatch",
  "version": "1.0.0",
  "description": "인증 구인구직 플랫폼",
  "private": true,
  "workspaces": [
    "frontend/web",
    "mobile_front",
    "Server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:web\" \"npm run dev:mobile\"",
    "dev:backend": "cd Server && npm run start:dev",
    "dev:web": "cd frontend/web && npm run dev",
    "dev:mobile": "cd mobile_front && npm run start",
    "build": "npm run build:backend && npm run build:web",
    "build:backend": "cd Server && npm run build",
    "build:web": "cd frontend/web && npm run build",
    "test": "npm run test:backend && npm run test:web",
    "test:backend": "cd Server && npm run test",
    "test:web": "cd frontend/web && npm run test",
    "lint": "npm run lint:backend && npm run lint:web",
    "lint:backend": "cd Server && npm run lint",
    "lint:web": "cd frontend/web && npm run lint",
    "install:all": "npm install && cd Server && npm install && cd frontend/web && npm install && cd mobile_front && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}